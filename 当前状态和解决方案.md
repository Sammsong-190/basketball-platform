# 当前状态和解决方案

## 📊 当前状态

- ✅ MySQL 客户端已安装（通过 Anaconda，版本 5.7.24）
- ❌ MySQL 服务器服务未运行
- ❌ Homebrew 的 MySQL 服务未安装

## 🎯 解决方案（三选一）

### 方案一：安装 MariaDB（推荐 ⭐）

**优势：**
- 完全兼容 MySQL
- 更轻量，安装更快
- 易于管理

**步骤：**
```bash
# 1. 安装 MariaDB
brew install mariadb

# 2. 启动服务
brew services start mariadb

# 3. 创建数据库
mysql -u root -p
CREATE DATABASE basketball_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# 4. 配置 .env
# DATABASE_URL="mysql://root:密码@localhost:3306/basketball_platform"

# 5. 初始化
npm run db:push
```

### 方案二：安装 MySQL（官方）

**步骤：**
```bash
# 1. 安装 MySQL
brew install mysql

# 2. 启动服务
brew services start mysql

# 3. 安全设置（可选，设置 root 密码）
mysql_secure_installation

# 4. 创建数据库
mysql -u root -p
CREATE DATABASE basketball_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# 5. 配置 .env
# DATABASE_URL="mysql://root:密码@localhost:3306/basketball_platform"

# 6. 初始化
npm run db:push
```

### 方案三：使用 Docker（如果已安装 Docker）

**步骤：**
```bash
# 1. 启动 MySQL 容器
docker run --name mysql-basketball \
  -e MYSQL_ROOT_PASSWORD=yourpassword \
  -e MYSQL_DATABASE=basketball_platform \
  -p 3306:3306 \
  -d mysql:8.0

# 2. 配置 .env
# DATABASE_URL="mysql://root:yourpassword@localhost:3306/basketball_platform"

# 3. 初始化
npm run db:push
```

## 🚀 推荐操作流程

### 快速开始（使用 MariaDB）

```bash
# 一键安装和启动
brew install mariadb && brew services start mariadb

# 创建数据库（首次需要设置密码或使用空密码）
mysql -u root -p << EOF
CREATE DATABASE IF NOT EXISTS basketball_platform 
CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
SHOW DATABASES;
EOF

# 配置 .env 文件
# 编辑 .env，添加：
# DATABASE_URL="mysql://root:你的密码@localhost:3306/basketball_platform"

# 初始化数据库
npm run db:generate
npm run db:push
npm run db:test
```

## 📝 .env 文件配置示例

```env
# 如果 root 没有密码
DATABASE_URL="mysql://root@localhost:3306/basketball_platform"

# 如果 root 有密码（替换 your_password）
DATABASE_URL="mysql://root:your_password@localhost:3306/basketball_platform"

# JWT Secret
JWT_SECRET="basketball-platform-secret-key-2024"
```

## ✅ 验证安装

```bash
# 检查服务状态
brew services list | grep -E "(mysql|mariadb)"

# 测试连接
mysql -u root -p -e "SHOW DATABASES;"

# 测试 Prisma 连接
npm run db:test
```

## 🆘 常见问题

### 问题 1: 安装失败
```bash
# 更新 Homebrew
brew update
# 然后重试安装
```

### 问题 2: 服务无法启动
```bash
# 检查端口占用
lsof -i :3306

# 查看日志
brew services list
```

### 问题 3: 连接被拒绝
- 确保服务正在运行：`brew services list`
- 检查防火墙设置
- 确认端口 3306 未被占用

## 📚 相关文档

- `安装MySQL指南.md` - 详细安装说明
- `使用现有MySQL.md` - 使用现有 MySQL 的指南
- `数据库连接步骤.md` - 连接步骤

## 🎯 下一步

1. **选择安装方案**（推荐 MariaDB）
2. **安装并启动服务**
3. **创建数据库**
4. **配置 .env 文件**
5. **运行 `npm run db:push`**

完成这些步骤后，数据库就连接好了！🎉
