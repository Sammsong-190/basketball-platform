========================================
🚀 快速启动步骤
========================================

请在终端中依次执行以下命令：

1. 进入项目目录：
   cd "/Users/song666/Desktop/未命名文件夹"

2. 手动创建 .env 文件（如果还没有）：
   在编辑器中创建 .env 文件，内容如下：
   
   DATABASE_URL="file:./dev.db"
   JWT_SECRET="basketball-platform-secret-key-2024"

3. 安装依赖：
   npm install

4. 生成 Prisma 客户端：
   npm run db:generate

5. 初始化数据库：
   npm run db:push

6. 启动开发服务器：
   npm run dev

7. 在浏览器中打开：
   http://localhost:3000

========================================
📝 如果遇到问题
========================================

- npm install 失败：尝试使用 npm install --legacy-peer-deps
- 端口被占用：修改 package.json 中的 dev 脚本添加 -p 3001
- 数据库错误：删除 dev.db 文件后重新执行 npm run db:push

========================================
