# 🚀 项目启动指南

## 第一步：安装依赖

在项目根目录执行：

```bash
npm install
```

这将安装所有必需的依赖包（Next.js、React、Prisma等）。

## 第二步：配置环境变量

创建 `.env` 文件（如果还没有的话）：

```bash
# Database - MySQL
# 格式: mysql://用户名:密码@主机:端口/数据库名
DATABASE_URL="mysql://root:password@localhost:3306/basketball_platform"

# JWT Secret
JWT_SECRET="basketball-platform-secret-key-change-in-production-2024"

# NextAuth (if needed later)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="basketball-nextauth-secret-change-in-production"
```

**注意**: 使用 MySQL 前需要：
1. 安装 MySQL 服务器
2. 创建数据库: `CREATE DATABASE basketball_platform;`
3. 确保 MySQL 服务正在运行

或者手动创建 `.env` 文件并复制上述内容。

## 第三步：初始化数据库

执行以下命令生成 Prisma 客户端并创建数据库：

```bash
# 生成 Prisma 客户端
npm run db:generate

# 推送数据库模式到数据库
npm run db:push
```

这将：
- 根据 `prisma/schema.prisma` 生成 Prisma 客户端
- 创建 SQLite 数据库文件 `dev.db`
- 创建所有数据库表

## 第四步：启动开发服务器

```bash
npm run dev
```

服务器将在 http://localhost:3000 启动。

## 🎯 快速验证

启动成功后，您可以：

1. 访问首页：http://localhost:3000
2. 注册新用户：http://localhost:3000/register
3. 登录：http://localhost:3000/login
4. 查看商品：http://localhost:3000/products
5. 查看社区：http://localhost:3000/posts

## 📝 其他有用命令

### 查看数据库（Prisma Studio）
```bash
npm run db:studio
```
这将打开一个图形界面，方便查看和管理数据库数据。

### 创建数据库迁移
```bash
npm run db:migrate
```

### 构建生产版本
```bash
npm run build
npm start
```

## ⚠️ 常见问题

### 1. 安装依赖失败
- 确保 Node.js 版本 >= 18
- 清除缓存：`npm cache clean --force`
- 删除 `node_modules` 和 `package-lock.json` 后重新安装

### 2. 数据库错误
- 确保 `.env` 文件中的 `DATABASE_URL` 配置正确
- 删除 `dev.db` 文件后重新执行 `npm run db:push`

### 3. 端口被占用
- 修改 `package.json` 中的 dev 脚本为：`"dev": "next dev -p 3001"`

## 📚 API 测试

### 测试注册接口
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@example.com","password":"123456"}'
```

### 测试登录接口
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'
```

### 测试需要认证的接口
将返回的 token 添加到请求头：
```bash
curl http://localhost:3000/api/users/profile \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

## 🎉 下一步

项目启动后，您可以：
1. 探索前端页面
2. 测试 API 接口
3. 查看数据库数据
4. 根据需求添加更多功能

祝开发愉快！🏀
